
// This returns the first element within the document that matches the specified slector, or group of selectors
var todaysDate = document.querySelector("#todaysDate")

var todo = document.

$( document ).ready( function() {
});
    
    moment().format('MMMM Do YYYY, h:mm:ss a'); 
    moment().format('dddd');                   
    console.log(format);
}
